# Usage — Savonius Mrf Transient

Run commands from this case directory:

```bash
cd savonius_mrf_transient
./Allclean
./Allrun-pre
./Allrun
```

Generated mesh, solver logs, processor folders and time directories are intentionally excluded from Git.
Re-run the case locally with OpenFOAM to regenerate them.
